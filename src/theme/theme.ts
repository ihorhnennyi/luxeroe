import { createTheme } from '@mui/material/styles'

const theme = createTheme({
	palette: {
		mode: 'dark',
		background: {
			default: '#1A1918',
			paper: '#1A1918',
		},
		text: {
			primary: '#fff',
		},
	},
})

export default theme
